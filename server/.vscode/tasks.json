{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-build",
            "type": "docker-build",
            "platform": "python",
            "dockerBuild": {
                "context": "${workspaceRoot}"
            }
        }
    ]
  }